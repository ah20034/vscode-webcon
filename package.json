{
  "name": "webcon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "tunnel": "npx --yes localtunnel --port 3000",
    "tunnel:lt": "npx --yes lt --port 3000",
    "tunnel:lt:me": "npx --yes lt --host https://localtunnel.me --port 3000",
  "tunnel:lt:log": "npx --yes lt --port 3000 --local-host localhost --print-requests",
    "tunnel:cf": "cloudflared tunnel --url http://localhost:3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "localtunnel": "^2.0.2",
    "nodemon": "^3.1.10"
  }
}
